{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Iniciar API (FastAPI)",
            "type": "shell",
            "command": "uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload",
            "isBackground": true,
            "problemMatcher": [],
            "presentation": {
                "reveal": "always",
                "panel": "dedicated"
            },
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}
